# TypeScript Review Summary - server/src/

**Reviewer:** typescript-reviewer (via task agent)
**Date:** 2026-04-18
**Files Reviewed:** All TypeScript files in `server/src/`
**Last Updated:** 2026-04-18 (during review session)

---

## About This Review

This review was generated by running a TypeScript review agent that analyzed all files in `server/src/`. Issues were tracked and addressed incrementally during the session.

### Methodology

1. **Issues identified** by the review agent
2. **Fixed iteratively** - user chose which to address
3. **Build verification** - `npm run build` after each fix
4. **Doc updated** - Fixed issues removed, new issues added, partially fixed kept

### Patterns Observed

- **`as unknown as T`** pattern preferred over `as any` - signals intentional type bridging
- **Error handling in service vs controller** - controller catches all errors uniformly; services return promises without try/catch wrappers
- **Async/await with Promise.all** - for mapping arrays of async operations

---

## Remaining Issues

### 9. Query Alias Mismatch
**File:** `vehicle-instances.module.ts:85`
```typescript
'vehicle_instances.map.id = :mapId and map_vehicle_instances.player.id = :playerId'
```

**Problem:**
- `vehicle_instances.map` - entity has no `map` property (has `vehicle`, `start`, `end`)
- Inconsistent alias: `vehicle_instances` vs `map_vehicle_instances`

**Context:** The hardcoded query builder alias `'map_place_instances'` (line 166) suggests these queries may be remnants from a refactor where entity names changed.

**Suggested fix:** Change query to filter by `gameId` directly:
```typescript
'vehicle_instances.gameId = :mapId and vehicle_instances.playerId = :playerId'
```

---

### 12. Property Name Mismatch in PlaceInstanceMapper
**File:** `place-instance.module.ts:123`
```typescript
mapPlace: await this.placesService.findOne(placeId)
```

**Problem:**
- Entity `PlaceInstance` has `place: Place` property
- Mapper sets `mapPlace` which doesn't exist on the entity
- Result: `place` field will be `undefined` after mapping

**Context:** This looks like a copy-paste error from a different mapper (possibly from a game/map context where `mapPlace` made sense).

**Suggested fix:** Change `mapPlace` to `place`

---

## Fixed Issues Reference (for context)

These were addressed during the review session but are retained here for reference:

### Issue #1 - Missing Type on `update` Method Parameter
- `entity` parameter had no type
- Fixed by adding `DeepPartial<T>`

### Issue #2 - Unresolved Promise in `patch` Method
- `mapper.toDomain()` returned Promise but wasn't awaited
- Fixed by using `async` callback and `await`

### Issue #3 - Unresolved Promises in JobMapper.toDomain
- `start` and `end` assigned as unresolved Promises
- Fixed with `Promise.all([start, end])`

### Issue #4 - Swallowed Token Verification Error
- `getAuthToken` verified but discarded result, swallowed errors
- Fixed to validate and throw on mismatch

### Issue #5 - CORS Allows All Origins
- Added TODO comment for later configuration

### Issue #6 - Password Assignment Bypasses Type Safety
- Used `userDto['password']` bracket notation
- Fixed by using proper `UpdateUserDto.password` field

### Issue #7 - Mutating `assign()` Call
- Lodash `assign()` mutates first argument
- Fixed with spread syntax

### Issue #8 - Password Replacement Without Hashing
- `replace()` stored plaintext password
- Fixed to hash before update

### Issue #10 - Incorrect Logger.debug Call
- First argument was `process.env` instead of message
- Fixed by removing incorrect argument

### Issue #11 - Non-Async `delete` Method
- Added `async` keyword

### Issue #13 - Excessive `as any` Type Casting
- Replaced with proper TypeORM types: `FindOptionsOrder<T>`, `FindOptionsWhere<T>`
- Used `as unknown as T` for complex type conversions

### Issue #14 - Unresolved Promise in `handlePagedResults`
- `mapper.toDto()` async but not awaited in `.map()`
- Fixed with `async` callback in map and `await Promise.all()`

---

## Recommendations

1. **Fix Promise handling** - ensure all async operations are properly awaited
2. **Use `as unknown as T`** instead of `as any` for intentional type bridging
3. **Standardize entity/query alias naming** - queries should match actual entity structure
4. **Controller error handling** - keep services thin, let controller normalize all errors