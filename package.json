{
  "name": "trains",
  "version": "0.0.0",
  "scripts": {
    "server-start": "npm --prefix ./server start",
    "web-start": "npm --prefix ./web run dev",
    "start:local": "npm --prefix ./server run start:local",
    "start:k8s": "npm --prefix ./server run start:k8s"
  }
}
